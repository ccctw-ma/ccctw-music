{
  "name": "@ccctw-music/harmony",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "pnpm --filter @ccctw-music/web build && pnpm check",
    "check": "node scripts/check.mjs",
    "build:native": "echo \"Open apps/harmony in DevEco Studio and run hvigor assembleHap after SDK setup.\""
  }
}
